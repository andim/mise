cluster: "sbatch --cpus-per-task=1 --mem=8000MB --time=50 --output=slurm_out/%x-%A --job-name={rule} --parsable --ntasks-per-node=1 --nodes=1 --partition=bioth"
jobs: 50  # maximum jobs to run at once
